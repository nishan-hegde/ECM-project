<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Background should be transparent -->
    <stroke
        android:width="2dp"
        android:color="@color/primary" /> <!-- Border color -->
</shape>
